#!/usr/bin/env python3
"""claude-quiet: collapse file-edit diffs to one line."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent / "lib"))

from ccpatch import main            # noqa: E402
from patch_def import PATCH         # noqa: E402

if __name__ == "__main__":
    sys.exit(main(PATCH, sys.argv))
